// 문자형으로 형변환

let value = ture; // boolean
value = String(value); // 문자형으로 ture가 저장됨.

// 숫자형으로 형변환

// 자바스크립트에서는 수학과 관련된 함수와 같은 표현식에서 자동으로 형변환이 적용됨.

alert("6"/"2"); // 자동 형변환으로 문자열이 숫자로 자동으로 변환 값은 3

let str = "123"; // string
let num = Number(str); // str의 값을 숫자형으로 형변환 num에는 123이 저장됨.

// 숫자가 아닌 글자가 들어있는 문자열을 숫자형으로 변형시에는 NaN이 출력됨.

// boolean 형으로 변환
// 값이 있으면 True로 fales같은 값은 false로 
// 다만 "0"은 true임 

